# Stufi cleanup discord bot
## Steps
* put your DISCORD_TOKEN into the .env file
* type !clmsg [keyword] [count] to initiate the cleaning
    * keyword can contain spaces
    * count is the count of words that the bot will scan
